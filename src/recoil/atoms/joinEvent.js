import { atom } from "recoil";

export const joinJobKind = atom({
  key: "joinJobKind",
  default: "1",
});

// export const joinParticipation = atom({
//   key: "joinParticipation",
//   default: "",
// });

export const uidState = atom({
  key: "uidState",
  default: null,
});

export const joinCompany = atom({
  key: "joinCompany",
  default: "1",
});
